#ifndef READFILE_H
#define READFILE_H

#include <cctype>
#include <fstream>
#include <string>
#include <vector>
#include <exception>

extern void readFile(std::vector <std::string> &words, const std::string &path);

#endif // READFILE_H
